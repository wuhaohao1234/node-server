// import ConnectionMongo from './API/index'

// let connectionMongo: ConnectionMongo = ConnectionMongo.getInstance()

// connectionMongo.createMongo('mongodb://localhost:27017/', 'runoob').then(() => {
   
// })
let arr: any[] = []
let obj = {}
console.log(Object.prototype.toString.call(arr))